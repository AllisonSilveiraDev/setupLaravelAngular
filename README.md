# setup Laravel Angular

**commands**
- composer install
- cd resources/angular
- npm install
- php artisan serve
- npm run build
- colocar um ponto no inicio dos arquivos (env.example, editorconfig, gitignore, gitattributes)
- cp .env.example .env
- php artisan key:generate
 
